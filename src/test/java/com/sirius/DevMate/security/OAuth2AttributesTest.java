package com.sirius.DevMate.security;

import static org.assertj.core.api.AssertionsForClassTypes.assertThat;

public class OAuth2AttributesTest {
//    @Test
//    void googleMapping() {
//        var a = Map.of("sub", "123", "email", "a@b.com", "name", "Alice", "picture", "https://x/p.png");
//        var o = OAuth2Attributes.of("google", a);
//        assertThat(o.provider()).isEqualTo("google");
//        assertThat(o.providerId()).isEqualTo("123");
//        assertThat(o.email()).isEqualTo("a@b.com");
//        assertThat(o.name()).isEqualTo("Alice");
//        assertThat(o.avatarUrl()).isEqualTo("https://x/p.png");
//    }
//    @Test
//    void githubMapping() {
//        var a = Map.of("id", "999", "login", "octo", "avatar_url", "https://x/a.png");
//        var o = OAuth2Attributes.of("github", a);
//        assertThat(o.provider()).isEqualTo("github");
//        assertThat(o.providerId()).isEqualTo("999");
//        assertThat(o.email()).isNull(); // 이메일 비공개 가능
//        assertThat(o.name()).isEqualTo("octo"); // name 없으면 login 사용
//        assertThat(o.avatarUrl()).isEqualTo("https://x/a.png");
//    }
}
